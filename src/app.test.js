// src/App.test.js
import { render } from '@testing-library/react';
import { GifExpertApp } from './GifExpertApp';

test('renders <GifExpertApp /> without crashing', () => {
  render(<GifExpertApp />);
});
